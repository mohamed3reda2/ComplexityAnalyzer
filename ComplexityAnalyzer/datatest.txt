بالطبع! إليك 5 أمثلة على أكواد C++ مع تعقيد زمني مختلف لتجربة البرنامج:

---

### الكود 1: **O(1)**
```cpp
int main() {
    int x = 5; // عملية واحدة
    int y = x + 10; // عملية واحدة
    cout << y; // عملية واحدة
    return 0;
}
```

---

### الكود 2: **O(n)**
```cpp
int main() {
    int n = 10;
    for (int i = 0; i < n; i++) { // حلقة واحدة
        cout << i << " ";
    }
    return 0;
}
```

---

### الكود 3: **O(n^2)**
```cpp
int main() {
    int n = 5;
    for (int i = 0; i < n; i++) { // حلقة خارجية
        for (int j = 0; j < n; j++) { // حلقة داخلية
            cout << i * j << " ";
        }
    }
    return 0;
}
```

---

### الكود 4: **O(log n)**
```cpp
int main() {
    int n = 64;
    while (n > 1) { // التقسيم إلى النصف في كل دورة
        cout << n << " ";
        n /= 2;
    }
    return 0;
}
```

---

### الكود 5: **O(n * log n)**
```cpp
int main() {
    int n = 16;
    for (int i = 0; i < n; i++) { // حلقة O(n)
        int x = n;
        while (x > 1) { // حلقة O(log n)
            cout << x << " ";
            x /= 2;
        }
    }
    return 0;
}
```

---

### كيفية الاستخدام
انسخ هذه الأكواد واحدًا تلو الآخر إلى البرنامج الذي قمت ببنائه، واختبر التعقيد الزمني لكل كود. النتائج المتوقعة:

1. **الكود 1:** O(1)
2. **الكود 2:** O(n)
3. **الكود 3:** O(n^2)
4. **الكود 4:** O(log n)
5. **الكود 5:** O(n * log n)

إذا كانت هناك مشكلة في تحليل أي من هذه الأكواد، يمكننا تعديل البرنامج لتحسين دقة التحليل. 😊